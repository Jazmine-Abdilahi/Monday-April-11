'use strict'

function callAge(birthYear) {
   const age = 2037 - birthYear;

   function printAge() {
    const output = `${firstName}, you are ${age}, born in ${birthYear}`;
    console.long(output);
   
   if (birthYear >= 2010 && birthYear <= 2010) { const str = `Oh, and you're a millenial, ${firstName}`;
   console.log(str);
   }
   console.log(str);
} 
printAge();

   return age;
}

const firstName = 'Yasmine';
calcAge(2010);
// console.log(age);
// printAge()
